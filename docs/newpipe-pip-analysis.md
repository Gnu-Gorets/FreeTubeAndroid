# NewPipe PIP: описание по скриншоту

Источник: `/home/gorets/Downloads/tg/Screenshot_20260917-015856_Lawnchair.png`  
Разрешение исходного скриншота: `720×1600 px`.

## Общий вид

На домашнем экране Android поверх launcher отображается PIP-плеер NewPipe. Видеоблок центрирован по горизонтали и находится примерно в верхней половине экрана. Под видеоблоком видны иконки launcher, поэтому это именно floating/PIP layer, а не отдельный экран приложения.

- Внешний фон: `#000000`.
- Размер экрана: `720×1600 px`.
- На этом кадре размер PIP: примерно `580×325 px`, левый отступ `46 px`.
- На этом кадре верхняя граница видео: примерно `y=264 px`.
- На этом кадре нижняя граница видео: примерно `y=589 px`.
- Размер и положение окна изменяются системой при resize/перетаскивании; эти значения не являются фиксированными.
- Соотношение сторон: `16:9`.
- Окно не имеет видимой рамки, тени или отдельного заголовка.
- По бокам остаются черные поля примерно по `46 px`; PIP не растянут на всю ширину экрана.

## Слой под PIP

Под PIP виден launcher Android:

- черный фон;
- строки иконок приложений видны ниже PIP;
- нижний dock с четырьмя крупными иконками;
- PIP перекрывает часть строки иконок;
- над PIP остается пустая черная область;
- status bar остается сверху на черном фоне.

## Status bar

В верхней части скриншота, над PIP:

- время: `01:58`;
- системные индикаторы справа;
- заряд: `81%`.

PIP не скрывает status bar и не переводит экран в fullscreen.

## Видеокадр

Внутри PIP отображается горизонтальное видео:

- в кадре крупно показан монитор с городским/техническим изображением;
- видео затемнено из-за активных controls;
- масштабирование выполняется без черных полос по бокам;
- кадр полностью заполняет область `16:9`.

## Верхняя панель controls

Панель расположена поверх видео у верхнего края PIP. Текст белый, жирный, небольшой размер.

Слева направо отображаются:

1. `Russi...` - выбранная аудиодорожка; label обрезан многоточием.
2. `480p` - выбранное качество видео.
3. `1x` - текущая скорость.
4. `No Captions` - субтитры выключены.
5. Иконка fullscreen справа.

Элементы идут в одну горизонтальную строку. Между labels небольшие равномерные отступы. Верхняя панель не занимает отдельную полосу, а рисуется поверх видеокадра.

## Центральный control

В центре PIP находится большая белая кнопка Play в форме треугольника.

- кнопка расположена примерно по центру видео;
- под кнопкой нет круглого фона;
- наличие Play означает, что на момент снимка видео остановлено;
- размер иконки заметно больше остальных controls.

## Нижняя панель controls

В нижней части PIP расположены время и seek bar:

- слева: `00:41`;
- по центру: горизонтальный progress bar;
- справа: `07:28`;
- progress bar красный;
- круглый красный thumb расположен примерно на 9% длины;
- elapsed time и duration белые;
- seek bar проходит почти от левого до правого края.

## Точные видимые значения

| Поле | Значение |
|---|---|
| Audio track | `Russi...` |
| Quality | `480p` |
| Playback speed | `1x` |
| Captions | `No Captions` |
| Current position | `00:41` |
| Duration | `07:28` |
| Playback state | Paused |
| Controls state | Visible |
| Orientation | Portrait screen, landscape video |
| PIP width | `≈580 px` |
| PIP height | `≈325 px` |

## Что должно быть повторено в FreeTube

- floating video поверх launcher/другого приложения;
- центрированное изменяемое окно; на эталонном кадре ширина примерно `80.6%` экрана (`580 px` при ширине `720 px`);
- aspect ratio `16:9`;
- черный фон за пределами видео;
- controls поверх видеокадра, без отдельной панели;
- верхняя строка: audio track, quality, speed, captions, fullscreen;
- центральная Play/Pause;
- нижняя строка: current time, seek bar, duration;
- красный progress и красный thumb;
- белый текст controls;
- сохранение текущих значений при открытии PIP;
- paused state с отображением Play;
- видимость status bar и launcher вокруг PIP.

## Эталон кнопки

Эталон dev/desktop player: `tmp/pi-clipboard-93eb58b6-bf71-4acd-beec-b19fe70eb7a0.png`.

- PIP-кнопка находится в нижнем control row.
- Порядок справа налево: overflow/settings с `HD`, PIP, full window, fullscreen.
- PIP - вторая кнопка справа после settings.
- Иконка: контур прямоугольника с маркером PIP.
- Tooltip: `Enter picture-in-picture (i)`.

## Текущее состояние FreeTube на устройстве

Скриншот: `tmp/android-pip/freetube-current.png`.

- Видео открыто в portrait-экране FreeTube.
- Нижний control row показывает Play, volume, time, fit-screen, overflow/settings с `HD` и fullscreen.
- PIP был только в overflow menu, поэтому desktop-позиция не соблюдалась.

## Исправление позиции

В Android-режиме controls разделены так:

- главный player row: `overflow/settings → PIP → fullscreen`;
- меню settings: `Chapters`, `Loop`, `full window`, `Stats` и остальные настройки;
- PIP в меню settings отсутствует;
- full window в главном player row отсутствует;
- full window доступен только внутри settings.

Для audio-only видео PIP не добавляется. Desktop-разметка не меняется.

## Реализовано

- Android `MainActivity` объявлен как PIP-capable.
- Добавлен узкий `AndroidBridge.enterPictureInPicture()`.
- Для PIP передается aspect ratio `16:9`.
- HTML video/WebView не заменяются отдельным native player.

## Не утверждается по одному кадру

По исходному скриншоту нельзя определить timeout скрытия controls, точные границы resize, жесты перемещения окна, действия системной кнопки закрытия и поведение после fullscreen. Размер PIP должен оставаться системно изменяемым. Это проверяется отдельно на устройстве.
